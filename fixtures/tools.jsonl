{"name":"search_docs","description":"Full-text search across product documentation. Use when looking up API reference or how-to pages. Do not use for customer ticket history.","inputSchema":{"type":"object","properties":{"query":{"type":"string","description":"Search query string (keywords or phrase)."},"limit":{"type":"integer","description":"Max hits to return (1–50). Defaults to 10."}},"required":["query"]}}
{"name":"get_ticket","description":"Retrieve a support ticket by id. Use when you already know the ticket number. Prefer search_tickets when you only have a customer email.","inputSchema":{"type":"object","properties":{"ticketId":{"type":"string","description":"Ticket id, e.g. TCK-1042."}},"required":["ticketId"]}}
